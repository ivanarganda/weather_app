import React, { FC, useState } from 'react';
import imageMap from './../assets/mapSpain.png'

const SpainMap: FC = () => {

  

  return (
    <React.Fragment>
      <img src={imageMap} width='100%' alt="map of spain" />
    </React.Fragment>
  )
};

export default SpainMap; 